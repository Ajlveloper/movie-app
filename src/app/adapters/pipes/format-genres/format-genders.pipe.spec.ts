import { FormatGendersPipe } from './format-genders.pipe';

describe('FormatGendersPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGendersPipe();
    expect(pipe).toBeTruthy();
  });
});
