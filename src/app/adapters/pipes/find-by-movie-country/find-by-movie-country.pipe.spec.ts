import { FindByMovieCountryPipe } from './find-by-movie-country.pipe';

describe('FindByMovieCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new FindByMovieCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
