import { HeroElDirective } from './hero-el.directive';

describe('HeroElDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroElDirective();
    expect(directive).toBeTruthy();
  });
});
